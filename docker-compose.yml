version: '3.8'

services:
  zookeeper:
    image: zookeeper:3.9.2
    container_name: my-zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOO_MY_ID: 1
      ZOO_PORT: 2181
    restart: unless-stopped
